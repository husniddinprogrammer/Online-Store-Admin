1-Task

Role: Senior frontend engineer + UI/UX designer

Context:
We are building a modern admin dashboard for an e-commerce system.
Frontend must integrate with an existing backend API.

Tech Stack:
- Next.js 14 (App Router)
- TypeScript
- Tailwind CSS
- Shadcn/ui (for modern components)
- React Query (TanStack Query)
- Axios (API client)
- Zustand (state management)
- React Hook Form + Zod (validation)
- Framer Motion (animations)

Design Requirements:
- Modern Material-style UI
- Clean, minimal, professional look
- Dark mode support
- Responsive (mobile + desktop)

Layout:
- Sidebar (left, fixed)
- Top navbar (search, notifications, user menu)
- Main content area

Pages:

1. Login Page:
- Email + password form
- JWT authentication
- Store tokens (access + refresh)
- Redirect after login

2. Dashboard:
- Statistics cards:
  - total users
  - total orders
  - total revenue
- Recent orders
- Notifications

3. Users Page:
- Table with pagination
- Search + filter
- Block/unblock user
- Change role

4. Products Page:
- Table/grid view
- Add/Edit/Delete product
- Image preview

5. Orders Page:
- Orders list
- Status update (PENDING → DELIVERED)
- Filter by status

6. Categories & Companies:
- CRUD operations

7. Cart (optional view for admin debugging)

8. Comments / Reviews:
- List + delete

9. Notifications:
- List + mark as read

10. Admin Management:
- Create admin user
- Assign roles

API Integration:
Use the provided API documentation:
API_DOCS.md file 
Requirements:
- Create reusable API service (axios instance)
- Add JWT interceptor (attach token automatically)
- Handle refresh token logic
- Handle errors globally
- Use React Query for fetching/mutations

Architecture:
- Feature-based folder structure
- Separate components, hooks, services
- Custom hooks (useUsers, useProducts, etc.)
Accessibility:
- Keyboard navigation
- Proper contrast
- ARIA labels

Components:
- Sidebar (navigation)
- Navbar
- DataTable (reusable)
- Modal (add/edit)
- Form components
- Toast notifications
- Loading skeletons

Performance:
- Lazy loading
- Code splitting
- Optimized rendering

Output:
- Full project structure
- Layout (sidebar + navbar)
- Login page
- Dashboard page
- Example API integration (axios + react query)
- Example pages (Users, Products)
- Reusable components
- Clean production-ready code

Important:
Act like a senior frontend engineer from a top tech company.
Avoid beginner mistakes.
Write scalable and maintainable code.

2-Task
Role: Senior frontend engineer

Task:
Build a product image upload component for admin panel

Tech Stack:
- Next.js 14 (App Router)
- TypeScript
- Tailwind CSS
- React Hook Form
- Axios
- React Query

Requirements:

Upload:
- Allow selecting multiple images
- Validate each image:
  - Max size: 3MB
  - Allowed types: jpg, jpeg, png

UI:
- Show image preview before upload
- Show remove button for each image
- Show upload progress (progress bar)
- Show error messages

API Integration:
Use this endpoint:
/**
     * POST /api/product-images/upload/{productId}
     * Content-Type: multipart/form-data
     *
     * Form fields:
     *   files       — one or more image files (jpg/jpeg/png, max 3MB each)
     *   firstIsMain — optional boolean; if true, the first file becomes the main image (default: true)
     */
    @PostMapping(value = "/upload/{productId}", consumes = MediaType.MULTIPART_FORM_DATA_VALUE)
    @PreAuthorize("hasAnyRole('SUPER_ADMIN', 'ADMIN')")
    @SecurityRequirement(name = "bearerAuth")
    @Operation(summary = "Upload images for a product (jpg/jpeg/png, max 3MB each)")
    public ResponseEntity<ApiResponse<List<ProductImageResponse>>> uploadImages(
            @PathVariable Long productId,
            @RequestParam("files") List<MultipartFile> files,
            @RequestParam(value = "firstIsMain", defaultValue = "true") boolean firstIsMain) {
        return ResponseEntity.status(HttpStatus.CREATED)
                .body(ApiResponse.success("Images uploaded",
                        productImageService.uploadImages(productId, files, firstIsMain)));
    }

Request:
- multipart/form-data
- files: array of images

Logic:
- Create FormData
- Append multiple files
- Send via axios
- Show loading state
- Handle success & error

State:
- Manage selected files
- Manage upload progress
- Manage errors

UX:
- Drag & drop support (optional)
- Disable upload if no files
- Show success toast

Output:
- Upload component
- API service function
- Example usage in Product page

Important:
- Clean and reusable code
- Use TypeScript properly
- Follow best practices
- Make it production-ready
3-TASK
Role: Senior frontend engineer

Task:
Build Category and Company create forms with image upload

Tech Stack:
- Next.js 14 (App Router)
- TypeScript
- Tailwind CSS
- React Hook Form
- Zod validation
- Axios
- React Query

Context:
Backend now accepts image file instead of imageLink.

API:
POST /api/categories
POST /api/companies
# Create category
curl -X POST http://localhost:8080/api/categories \
  -H "Authorization: Bearer <token>" \
  -F 'data={"name":"Electronics"};type=application/json' \
  -F "image=@logo.jpg"

# Create company (no image — image is optional)
curl -X POST http://localhost:8080/api/companies \
  -H "Authorization: Bearer <token>" \
  -F 'data={"name":"Samsung"};type=application/json'


Request:
multipart/form-data
- name: string
- image: file

Requirements:

Form:
- Input: name
- Input: image (file upload)

Validation:
- Required fields
- Image max size: 3MB
- Allowed types: jpg, jpeg, png

UI:
- Modern material design
- Image preview before upload
- Remove selected image button
- Error messages
- Loading state
- Success toast

Upload Logic:
- Use FormData
- Append name + image
- Send via axios
- DO NOT use JSON
- Use multipart/form-data

State:
- Manage image file
- Manage preview URL
- Manage loading & errors

UX:
- Disable submit if invalid
- Show upload progress (optional)
- Show success message

Architecture:
- Separate component
- Reusable form logic
- Clean code

Output:
- CategoryCreate component
- CompanyCreate component
- API service function
- Form validation
- Upload logic

Important:
- Use best practices
- Avoid beginner mistakes
- Make it production-ready
Also:
- Add drag & drop support
- Show image preview as thumbnail
- Limit to 1 image only
- Show file size warning